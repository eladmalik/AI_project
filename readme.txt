how to run:
1. make the files executable:
chmod +x make.sh
chmod +x run_example_test.sh
chmod +x run_example_train.sh

2. run the make.sh to install the requirements
3. run run_example_train.sh to see a training example
4. run run_example_test.sh to see an example of a trained model